Hello, <%= @user.email %>!

There are <%= @expired_items.size %> expired items.
<% @expired_items.each.with_index(1) do |expired_item, index| %>
<%= "#{index}. #{expired_item.name} (#{expired_item.expiration})" %>
<% end %>

There are <%= @expiring_items.size %> items expiring in the next week.
<% @expiring_items.each.with_index(1) do |expiring_item, index| %>
<%= "#{index}. #{expiring_item.name} (#{expiring_item.expiration})" %>
<% end %>

Check out all your items at <%= foods_url %>

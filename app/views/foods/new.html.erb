<%= error_messages_for @food, class: 'alert-danger' %>

<%= form_for @food do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.label :expiration %>:
  <%= f.date_field :expiration %><br />

  <%= f.submit %>
<% end %>
